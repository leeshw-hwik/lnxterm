# LnxTerm 구현 계획

ST-Link V3 Mini 기반 임베디드 장치 디버깅/로그 수집용 시리얼 터미널 GUI.

## 현재 릴리스 범위
- 버전: **v1.8.4**
- 목표: 환경 변수 기반 데이터(문자열 통계, 자동 명령) 자동 로딩 및 자동화 기능 강화

## 기술 스택
- Python 3.12 + PyQt6 + pyserial + python-dotenv
- PyInstaller (`./build_exe.sh`)
- GitHub CLI(`gh`) 릴리스 관리

## 핵심 기능 현황

| 기능 | 상태 |
|------|------|
| 시리얼 통신 | 포트 스캔/연결/해제/QThread 수신 |
| 자동 재연결 | 환경변수 기반 주기(`MS`/`SEC`), 기본 3초 |
| 포트 점유 감지 | Lockfile + `fuser`/`lsof` + `TIOCEXCL` |
| 로그 파일 | 세션 고정 파일 유지, 재연결 append 재개 |
| 통계 CSV | 로그 파일 기반 경로 고정, 재연결 후 누적 지속 |
| 자동 로딩 | `.env`에서 문자열 통계 및 자동 명령 사전 설정 로드 |
| 자동 저장 | 메뉴/종료 시 현재 설정을 `.env`에 자동 저장 (IGNORE 모드 제외) |
| 로딩 모드 | ALWAYS (무조건), IGNORE (무시), CONFIRM (사용자 확인) 지원 |
| 자동 명령 | 트리거 기반 사후 명령 실행 (Delay, Interval 지원) |

## v1.8.4 완료 항목
1. `.env.example`에 자동 로딩 설정 변수 추가
2. `SidebarWidget`에 환경 변수 파싱 및 데이터 로드/저장 로직 구현
3. 프로그램 시작 시 로딩 모드(ALWAYS/IGNORE/CONFIRM) 처리
4. 자동 명령(JSON 형식) 및 문자열 통계 키워드 로딩/저장 기능 추가
5. 메뉴(`파일` -> `환경 변수 업데이트`) 및 종료 시 `.env` 자동 업데이트 기능 추가

## 잔여/후속 항목
1. 자동 명령 수행 안정성 및 성능 테스트 (고빈도 로그)
2. UI 컴포넌트 분리 (Large Class 리팩토링)
3. 장시간 로깅 시 파일 I/O 안정성 점검

## 변경 이력
- **v1.8.0**: 사이드바/터미널 개선, 문자열 통계 기능 도입
- **v1.8.1**: 통계 CSV 스키마 고정 및 문서 정리
- **v1.8.2**: `.env`/재연결/로그·통계 세션 지속성 개선
- **v1.8.3**: UI 세밀화 및 자동 명령 개수 제한 추가
- **v1.8.4**: 환경 변수 기반 데이터 자동 로딩 및 저장 기능 추가
